# VITE_API_URL is intentionally left empty so the frontend uses relative API paths.
# In development, Vite's server.proxy handles /api forwarding.
# In production (Docker/nginx), /api is proxied to the backend service.